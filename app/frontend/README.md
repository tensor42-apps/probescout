# Frontend (Node.js)

All frontend code resides here. Node.js-based. View layer only: displays state from backend; no business decisions or scan logic here.

See `docs/implementation_design.md` (frontend section) for UI spec. Target input, Execute button, status box with stages and nmap output.
